/:
# necessary directories
  succ: keep
  tmp: keep
# common directories
  bin: replace
  boot: keep
  dev: keep
  etc: replace
  home: keep
  lib: replace
  lib64: replace
  mnt: keep
  opt: replacewipe
  proc: keep
  root: replace
  run: keep
  sbin: replace
  srv: replace
  sys: keep
  usr: replace
  var: keep
# special directories
  lost+found: keep
